function Header() {

  const headerStyle = {
    backgroundColor: "darkblue",
    color: "white",
    textAlign: "center",
    padding: "20px"
  };

  return (
    <header style={headerStyle}>
      <h1>Setup React</h1>
    </header>
  );
}

export default Header;